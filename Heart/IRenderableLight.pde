/**
 * <code>IRenderableLight</code> describes interface of light class which could be rendered to scene.
 * @author Vitaly Obukhov
 * @version 1.0
 */
public interface IRenderableLight
{
  /**
   * Renders current light to scene.
   */
  void Render();
}
